services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    # TODO: add a postgres service once src/db exists and something reads a DB connection string
    # TODO: add parser/worker/webhook-api services once src/parser, src/worker, src/api exist
